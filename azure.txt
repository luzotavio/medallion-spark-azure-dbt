# CONFIGS

> Começamos criando um **Resource Group**, que é uma forma de agrupar todos os recursos que vamos utilizar do azure, para organizar tudo
> 

depois criamos um ***gen 2***, que é onde os dados vão ficar, linkamos com o resource group

marcamos a opção de pastas hierarquicas, que permitem criar diretórios reais no storage

depois criamos os containers, que são as camadas do nosso projeto(bronze, silver e gold)

agora criamos o ***Data Factory***, que é onde vamos orquestrar o fluxo dos dados

criamos as keys vaults, serve para guardar as senhas do nosso projeto com nome amigáveis, quando colocamos por exemplo no código, apenas apontamos para o nome da senha na key vault = segurança

criamos o azure sql database com dados ficticios que o próprio azure cria, como nossa fonte de dados, também criamo o server

no data factory, criamos um linked service, que é uma conexão automática para fontes de dados

### pipeline

configuramos um tal lookup, que vai até a fonte de dados e nos retorna o que tem lá dentro

depois colocamos o foreach, que faz determinada ação para cada elemento retornado pelo nosso lookup, no caso os nomes das tabelas

configuramos o copy data, dinamicamente para receber e o schema e o nome da tabela de cada loop do for each dinamicamente

configuramos o sink do copy data para salvar os dados na camada bronze e salvar os dados com a data daquele arquivo

logo após toda configuração fazemos um debug

tudo certo, o adf puxa os dados do azure sql database por meio do lookup, ele puxa somente os schemas e tablesnames, o for each recebe essa lista, e por meio dos parametros, ele pega cada schema e nome, vai no banco de dados, copia aquele dado, cria uma pasta com a data atual, e carrega cada tabela nessa pasta em formato parquet, tudo isso no gen2 e no conteiner bronze

essa parte que fazemos foi o E e Ldo elt, extraimos e carregamos o dado no nosso data lake, agora vamos para o t, que vai ser no databricks

### DATABRICKS - TRANSFORM

primeiro criamos um azure databricks workspace, que é uma integração do azure com o databricks, o azure cuida de toda parte da infraestrutura, o cluster em  si e o databricks é o cérebro, é a parte da lógica, configuração, etc

criamos uma pasta no workspace principal, e criamos um notebook base

criamos um key no key vault, com a key de acesso ao storagge acoount e linkamos ele no databricks

criamos um cluster, escolhemos o runtime, que é como se fosse o sistema operacional com as versões do software e também o tipo de node e número de cores

rodamos o código abaixo para criar uma conexão de fato com o data lake

```python
spark.conf.set(
    "fs.azure.account.key.medallionlo.blob.core.windows.net",
    dbutils.secrets.get(scope="databricksScope", key="storageAccountKey")
)

path_bronze = "wasbs://bronze@medallionlo.blob.core.windows.net"
path_silver = "wasbs://silver@medallionlo.blob.core.windows.net"
path_gold   = "wasbs://gold@medallionlo.blob.core.windows.net"
```

depois adicionamos o notebook do databricks no datafactory, criamos o linked service e 3 parametros para ler o nome do arquivo completo

enfrentei alguns probelmas com essa notebook aparentemente por causa da auenticação da chave, tive que usar outro método para conectar com o gen2, com a ajuda do claude

o que fizemos foi criar um database dentro do databricks que apenas referencia os dados dentro do gen2

depois criamos o dbt localmente, conectamos ele no databricks, criamos os snapshots para a camada silver, com os metadados criados pelo dbt